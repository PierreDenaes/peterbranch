{% extends 'base.html.twig' %}

{% block title %}PostComment{% endblock %}

{% block body %}
    <h1>PostComment</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ post_comment.id }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ post_comment.content }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ post_comment.createdAt ? post_comment.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_post_comment_index') }}">back to list</a>

    <a href="{{ path('app_post_comment_edit', {'id': post_comment.id}) }}">edit</a>

    {{ include('post_comment/_delete_form.html.twig') }}
{% endblock %}
